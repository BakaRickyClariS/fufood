# 專案架構說明 (Project Structure)

本文件旨在協助開發者快速理解 FuFood 專案的目錄結構與各資料夾用途。

> **最後更新日期**: 2025-11-27
> **架構風格**: Feature-based (功能模組化) + Shared (共用資源)

---

## 📂 目錄結構總覽

```
src/
├── assets/                 # 靜態資源 (圖片、Icon 等)
├── features/               # ✨ 功能模組 (核心業務邏輯)
│   ├── auth/               # 身份驗證 (登入、註冊)
│   ├── dashboard/          # 儀表板 (首頁概覽)
│   ├── food-scan/          # 食材掃描 (OCR、圖片辨識)
│   ├── inventory/          # 庫存管理 (食材列表、分類)
│   ├── recipe/             # 食譜推薦 (AI 建議)
│   └── settings/           # 系統設定 (個人資料、偏好)
├── routes/                 # 🚀 頁面路由 (Page Components)
│   ├── Auth/               # 驗證相關頁面
│   ├── Dashboard/          # 儀表板頁面
│   ├── FoodInput/          # 食材輸入頁面
│   ├── Inventory/          # 庫存頁面
│   ├── Recipe/             # 食譜頁面
│   └── Settings/           # 設定頁面
├── shared/                 # 🔄 共用資源 (跨功能使用)
│   ├── components/         # 共用 UI 元件 (Buttons, Cards, Layouts)
│   ├── constants/          # 共用常數 (Routes, API Endpoints)
│   ├── hooks/              # 共用 Hooks (useWindowSize 等)
│   ├── types/              # 共用 TypeScript 型別
│   └── utils/              # 共用工具函式 (Formatters, Validators)
├── lib/                    # 第三方套件設定 (Cloudinary, Axios 等)
├── style/                  # 全域樣式 (CSS, Tailwind 設定)
├── Layout.tsx              # 根版面配置 (Root Layout)
├── main.tsx                # 進入點 (Entry Point)
└── Router.tsx              # 路由定義
```

---

## 🏗️ 詳細資料夾用途說明

### 1. `src/features/` (功能模組)

這是專案的核心，採用 **Feature-based** 架構。每個功能資料夾自成一格，包含該功能所需的所有資源。

| 資料夾 | 說明 | 包含內容範例 |
| :--- | :--- | :--- |
| **auth** | 身份驗證模組 | `LoginForm`, `useAuth`, `authService` |
| **dashboard** | 儀表板模組 | `OverviewPanel`, `RecentItems` |
| **food-scan** | 食材掃描模組 | `CameraCapture`, `OcrService` |
| **inventory** | 庫存管理模組 | `InventoryList`, `CategoryTabs`, `inventoryService` |
| **recipe** | 食譜模組 | `RecipeCard`, `AiRecommendation` |
| **settings** | 設定模組 | `ProfileForm`, `ThemeSwitcher` |

**Feature 內部結構通常包含：**
- `components/`: 該功能專屬的 UI 元件
- `hooks/`: 該功能專屬的 Logic Hooks
- `services/`: API 呼叫與資料處理
- `store/`: 狀態管理 (Redux/Zustand slice)
- `types/`: 專屬型別定義
- `constants/`: 專屬常數

### 2. `src/shared/` (共用資源)

存放跨多個 Feature 使用的資源，避免重複程式碼。

| 資料夾 | 說明 |
| :--- | :--- |
| **components/ui** | 基礎 UI 元件 (通常來自 shadcn/ui)，如 `Button`, `Input` |
| **components/common** | 專案通用的複合元件，如 `PageHeader`, `LoadingSpinner` |
| **components/layout** | 版面配置元件，如 `Navbar`, `Sidebar`, `Container` |
| **utils** | 工具函式庫，如 `formatDate`, `validateEmail` |
| **hooks** | 通用 Hooks，如 `useDebounce`, `useLocalStorage` |

### 3. `src/routes/` (頁面路由)

對應到 URL 的頁面層級元件 (Page Components)。
這些元件主要負責：
1. 接收路由參數 (Route Params)
2. 組合 Feature 元件
3. 處理頁面層級的 Layout

### 4. `src/lib/` (第三方設定)

存放第三方服務或套件的初始化與設定檔。
- 例如：`cloudinary.ts` (圖片上傳服務設定)

---

## 💡 開發指引

1. **新增功能時**：
   - 請在 `src/features/` 下建立新的功能資料夾。
   - 盡量將邏輯封裝在 Feature 內部，不要洩漏到全域。

2. **新增共用元件時**：
   - 如果是純 UI (無業務邏輯)，放 `shared/components/ui`。
   - 如果帶有特定業務邏輯但跨功能使用，放 `shared/components/common`。

3. **修改頁面時**：
   - 到 `src/routes/` 找到對應頁面。
   - 頁面應盡量保持乾淨，主要邏輯應抽離至 Feature 的 Hooks 或 Components 中。
